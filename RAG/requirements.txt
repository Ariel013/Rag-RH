# Core API & server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Client OpenAI-compatible (Groq / Ollama)
openai>=1.30.0

# Embeddings locaux via ONNX (rapide, ~90 MB, pas de PyTorch)
fastembed>=0.3.0

# PostgreSQL + pgvector (Supabase)
psycopg2-binary>=2.9.0
pgvector>=0.3.0

# HTTP client async (Supabase Storage)
httpx>=0.27.0

# Rate limiting
slowapi>=0.1.9

# Document parsers
pypdf>=3.0.0
python-docx>=0.8.11

# Notion API
notion-client>=2.2.1

# OCR pour les images Notion
pytesseract>=0.3.10
Pillow>=10.0.0
